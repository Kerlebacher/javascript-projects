// Create a string variable containing your name.
const word = 'Katelyn Erlebacher';

// Write a for loop that prints each character in your name on a different line.
for (let i = 0; i < word.length; i++) {
    console.log(word[i]);
}